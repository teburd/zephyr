load(":board.bzl", "declare_targets")

# Board data lives in board.bzl — loaded by boards/all.bzl.
# Build: buck2 build //samples/hello_world:zephyr --target-platforms //boards/qemu/x86:qemu_x86
declare_targets()
